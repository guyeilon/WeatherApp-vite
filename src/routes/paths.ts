enum Path {
  ALL = '*',
  NotFound = '404',
}

export enum ProtectedPath {
  ROOT = '/',
  Home = '/',
  Favorites = 'favorites',
}

export enum PublicPath {
  Login = 'login',
}

export default Path;
